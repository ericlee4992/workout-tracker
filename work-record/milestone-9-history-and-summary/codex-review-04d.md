# Codex cross-review 04d — Dumbbell reclassification, round 4

**clear.** Across `2207bd7..a208023`, the round-3 Standards and Spec findings are closed with nothing new introduced: the `(live-preset rank, capture time, stable entry UUID)` key is total for persisted entries, its leading rank guarantees that a live preset wins even over an older snapshot-only row, and the three distinct cyclic insertion orders in the new test put that live row first, middle, and last (the fourth iteration repeats the first order, but the set still exercises both relative orders against each snapshot row and is adequate for this regression). The projected fetch filters exclusively on `snapshotFreeWeightTag` before target fetching or any relationship, date, identifier, or other candidate property is accessed, so a rejected barbell row faults nothing else. `git diff --check 2207bd7...a208023` passes, and two independent runs of `DumbbellExercisesTests` passed 19/19. Standards — 0 findings; Spec — 0 findings.
