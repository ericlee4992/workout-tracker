# Ticket 13 — Round 1 (T6)

Reviewed `main..HEAD` on `history-13-save-as-template`: `fcef729` → `06c0274`. Static review only; the reviewer wrote only this report and ran neither `xcodebuild` nor `simctl`. Concurrent uncommitted work is outside this review boundary.

- **P3 — Missing design evidence:** `work-record/history-templates-and-zones/issues/13-save-as-template-from-history.md:28`. The two-line design note does not supply the job/state description, wireframe, tells, or default/AccessibilityL captures required by `.claude/skills/ios-design/SKILL.md` steps 1/2/4/6 and `REVIEW.md` item 12. In particular, existing History captures do not show the new saved-template confirmation. Add the record and matching captures so wrapping and grouping can be visually reviewed. This is an evidence gap, not an observed layout defect.

- **P3 — Claimed finish-save gate does not exercise that path:** `work-record/history-templates-and-zones/issues/13-save-as-template-from-history.md:36`. `HeartRateSummaryUITests` checks finishing, charts, and navigation to History; it never taps `saveAsTemplate`, edits the naming alert, or checks its confirmation. The only UI test invoking `saveAsTemplate` is the new History test. Correct the gate description and add a finish-sheet save regression covering the prefilled name, Save, and confirmation; the reported green tests do not establish the extraction's presentation behavior on that screen.

No introduced functional defect found. Finish-sheet copy and identifiers are preserved. Both callers start presentation false; the modifier is mounted independently of workout availability, and `RootView` supplies the finished workout before presenting the sheet. No reachable initial-true or late-workout case bypassing the default-name reset was found; SwiftUI alert timing was not runtime-verified. History edits mutate the same models consumed by both eligibility and saving; additions receive `completedAt`, and deletion paths save before another menu action. Current reps/order are captured; set type and load-type corrections are not template fields. The live exercise relationship defines a future generic template without replacing D23 historical display or grouping snapshots. The menu item's ellipsis preserves the finish button's existing copy, and the confirmation joins the name section's existing group. The far-end append and predicates are reasonable for this reset fixture, though prefix/suffix matching does not verify the exact workout date or retry behavior.
