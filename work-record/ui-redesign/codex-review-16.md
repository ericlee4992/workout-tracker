# Ticket 16 — Round 1 (T6)

Reviewed `main..HEAD` (`7a983fd..42627d5`) on `ui-redesign-16-active-workout` against `.claude/skills/ios-design/REVIEW.md` and the ticket's final Round 4 decision. Three P3 findings; not clear yet. The earlier directions do not override the user's decision to keep the current screen.

- **P3 — Expose seconds to VoiceOver.** `WorkoutTracker/Features/ActiveWorkout/ActiveWorkoutView.swift:349`: the new accessibility label replaces the seconds-bearing text with whole minutes: `0:59` becomes “Elapsed 0 minutes,” and `1:02:03` becomes “Elapsed 62 minutes.” Seconds were explicitly requested; they should also be available when a VoiceOver user focuses the clock. Derive the spoken hours/minutes/seconds from the same clamped elapsed value. This does not require automatic announcements every second. This is an accessibility parity defect, separate from item 11's test-query compatibility.

- **P3 — Capture the lower card and footer at AXL (items 9/12).** `WorkoutTrackerUITests/RedesignScreenshotUITests.swift:86`: after the new scroll-to-top swipes, the entry title is already hittable, so the loop does not scroll. `02-active-workout-axl.png` and `02-active-workout-axl-2.png` show essentially the same viewport, apart from live readings. Neither documents the complete set inputs, waiting second set, Add Set, or Add Exercise/Add by Machine. The ticket's description of `-axl-2` as “the card” at `work-record/ui-redesign/issues/16-active-workout-second-pass.md:122` overstates the coverage. Target lower controls, verify their position above the pinned rest bar, and take enough captures to record the whole card and footer. This is missing evidence, not proof those controls are broken.

- **P3 — Name the accepted hierarchy exception (items 1/12).** `work-record/ui-redesign/issues/16-active-workout-second-pass.md:104`: “the header no longer competes … at all” and “whatever it was in the first pass” do not identify the remaining bold element. **Add Exercise** is the large amber filled command (`ActiveWorkoutView.swift:125`); while resting, **Skip** is another amber filled command (`RestTimerBar.swift:43`). The completed set is amber state and explicitly excluded from item 1. The elapsed clock also remains a prominent `stat` figure, sharing the countdown's type treatment. Lines 100–101 honestly preserve the user's preference, but the restated hierarchy is incomplete: explicitly record Add Exercise and Skip as accepted competitors, and say the header's prominence is reduced. Preserve the chosen UI; this finding asks for an honest exception record, not another redesign.

The requested header is implemented: gym chip, seconds clock in `stat`, trailing neutral 22 pt ring and `N/M sets` caption; the Large Title hero and amber count chip are removed. The inspected AXL header keeps “Iron Temple,” the clock and sets caption whole. The rest controls sit together below the timer with whole “+15s” and “Skip” labels, fixing the observed mid-word wrap. Default and AXL use the same gym/exercise, one completed set, one pending set, live heart-rate fixture and running rest. The lower-content evidence gap above prevents clearing item 9 for the whole screen. The default finish capture remains coherent; finish source is unchanged.

Clock cost: `ActiveWorkoutView.swift:344` requests 60 times the previous update frequency, but the timeline closure contains only formatted clock text and its accessibility label; set traversal/counting at lines 337–338 is outside it. Apple's [TimelineView documentation](https://developer.apple.com/documentation/swiftui/timelineview) describes scheduled updates to contained content and gives a one-second timer example. Static inspection shows no new per-tick persistence, network work or whole-workout state mutation. This is a reasonable cost for requested seconds, not a demonstrated performance defect; no profiling was performed.

Clock skew is handled at display time: `elapsedSeconds` at `ActiveWorkoutView.swift:369` can return a negative integer for a future `startedAt`, but `Format.elapsed` at `WorkoutTracker/Domain/SharedEnums.swift:148` clamps it to zero. The spoken minutes helper separately clamps at `ActiveWorkoutView.swift:375`. Thus neither displays a negative elapsed duration. The three formatter tests cover zero, under-hour values, the hour boundary, hours/minutes/seconds, and negative input. Judgment: elapsed `h:mm:ss` from an hour and rest `m:ss` are consistent enough for their different jobs; the explicit hour format matches the recorded decision, and both use `m:ss` below an hour.

Item 11 passes for test-read strings and identifiers. Diff inspection and searches across `WorkoutTrackerUITests` found no query for the removed header “N min” or bare completed-count text. No accessibility identifier changed; the sets accessibility label, gym fallback, rest labels and queried action labels remain unchanged. Visible copy did change to the explicitly authorized seconds clock and `N/M sets` caption, so “no string changed” would be too broad. No additional finding in the changed scope for items 2–8 or 10; the neutral ring keeps the existing Reduce Motion-gated animation.

Validation was source, query and capture inspection only. The ticket reports `FormatElapsedTests` 3/3; that result was not independently rerun, and the named screenshot gates lack explicit pass counts in the ticket. No `xcodebuild` or `simctl` ran. No source files were modified.
