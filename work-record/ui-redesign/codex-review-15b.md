Clear — reviewed `a350944...af7a2b7` on `templates-15-delete-in-detail`. Both Round 1 P3s are resolved: the menu mechanism is explicitly suspected, and the ticket now records the job/state, inherited wireframe with the delete row, and tells. The view-owned `deleted` flag is set before delete/save and short-circuits the items and title reads during dismissal, addressing the post-save `isDeleted` limitation; no new correctness, design, copy, or test-code finding. Verification remains pending as stated: record the full-suite result from the previous commit and the TemplateDetailUITests/template-capture reruns on this commit before merge. No xcodebuild or simctl ran, and no source files were modified.
