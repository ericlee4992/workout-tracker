# Ticket 03 — Codex round 2 (T6)

Reviewed `48d225c..538483a` (`ui-redesign-03`, one commit), the round-1 response, and the committed AXL and heart-rate screenshots. No xcodebuild or simctl; no source changes. Reported suite results were not rerun. Independently executed the unchanged pure helper in a temporary Swift script for the cases below and 6,000 deterministic arithmetic checks.

- **P2 — New zero-width result can crash the zone card.** `WorkoutTracker/Features/ActiveWorkout/WorkoutFinishedSheet.swift:298` indexes `widths[index]` for every element of `present`, but `WorkoutTracker/Domain/ZoneBarLayout.swift:16` returns `[]` for nonempty input when width is zero. For example, `[10]` at width 0 returns zero widths for one present zone. Thus `present.count == widths.count` is not guaranteed: if the geometry is zero during layout, the new subscript traps. Preserve cardinality with zero-width segments or guard rendering before indexing, and update the degenerate-input test that currently expects the incompatible empty array. The original normal-width overflow arithmetic is fixed, but this introduced integration defect prevents closure.

- **P3 — The AXL capture does not show enough of the grid to verify truncation.** `WorkoutTrackerUITests/RedesignScreenshotUITests.swift:78` screenshots immediately after opening the receipt. In `screenshots/03/03-finish-summary-axl.png`, the first row's “Workout time” and “Active calories” labels wrap cleanly to two lines and their values fit. The image ends partway through the second row; its values and the entire maximum-heart-rate/volume row are outside the viewport. This is incomplete evidence, not proof of actual text truncation. Add bounded scrolling and enough AXL captures to show all six tiles, including the longer BPM values, before marking the two-column grid verified.

Other scope checks: `[3599, 1]` at 300 produces `[294, 4]` (sum 298); `[3595, 1, 1, 1, 1, 1]` produces `[270, 4, 4, 4, 4, 4]` (sum 290); one zone produces `[300]`; an unequal unaffordable-minimum case `[7, 1, 1, 1]` at 10 produces `[1, 1, 1, 1]` (sum 4). With finite positive width, nonnegative gap/minimum, and affordable gaps, widths remain within `[0, width]` and sum to width minus gaps within floating-point rounding (maximum observed error `5.7e-14`). “Always” needs that gap qualification: six zones at width 5 yield six zeros, while the five fixed 2-point gaps alone occupy 10 points. The unaffordable test's assertion is correct, although its diagnostic incorrectly says “7 shared four ways”; the remainder is 4.

The original **P3 chart capture finding is closed for the supplied fixture**: both loops allow at most six swipes, assert hittability, and use stable identifiers; the new screenshot visibly includes the complete plot, clock axis, and average caption, followed by a bounded return to View in History before tapping. Caption hittability is not a universal proof that the chart's top is visible on every viewport, but no remaining failure is demonstrated in this scoped flow. Maximum-heart-rate and volume existence assertions are present. No independent standards violations or other confirmed regressions were found.
