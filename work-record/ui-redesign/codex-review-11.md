# Codex review 11 — Round 1 (T6)

Reviewed `55d6562424f7e48949ea4844d895577eb9404ba3..6abc9e2d429358ec70df28ab88c4be71b06c0a86` on `ui-redesign-11-icons-and-template-detail`. Read the ios-design SKILL, REFERENCE and REVIEW, composition ticket, diff, relevant callers, tests and history. Opened the default/AXL Start and detail captures, both scrolled captures, active workout, History/list/detail, Gyms/detail and Exercises PNGs. No source modifications, xcodebuild or simctl; no runtime tests rerun. Contrast calculations used the source sRGB colours.

**Not clear against the full checklist: two P2 review-gate findings below. No demonstrated functional regression in the implementation.**

- **P2 — The new caption still needs the recorded user decision (item 11).** `WorkoutTracker/Domain/WorkoutTemplates.swift:55` and `WorkoutTracker/Features/Start/TemplateDetailView.swift:97` ship the new sets/reps sentence. `work-record/ui-redesign/issues/11-icons-and-template-detail.md:63` explicitly leaves that copy to the user; it does not record acceptance. This is an outstanding product decision, not a claim that the calculation is wrong. Record the decision on the actual caption, including missing-target/count-only variants, before calling item 11 clear. “Start” is existing copy, independently verified below.
- **P2 — AccessibilityL evidence does not cover the claimed layouts or all changed rows (items 9/12).** `WorkoutTrackerUITests/RedesignScreenshotUITests.swift:113` and `:143` create five standalone exercises yielding only Chest, Back and Legs. Thus the detail captures contain **no superset chip** and the three-icon strip stays on **one line**. They cannot substantiate the requested chip-stacks/strip-wraps checks, despite the ticket's wrapping claim at `issues/11-icons-and-template-detail.md:88`. Capture a matching default/AXL fixture with a superset and all five families, scrolling every named block into view. Also supply current AXL pairs for the changed active-workout, History-detail and ExerciseRow surfaces: ticket 11 contains only their default captures (`test02_activeWorkoutAndFinish` at `:38`, `test05_history` at `:199`, and `test06_gymsAndExercises`). Earlier screenshots with the old row icons do not verify this revision. The existing AXL detail's final row is fully readable after scrolling; this finding is missing evidence, not observed clipping.

**Checklist, items 1–12**

1. **Bold element — pass for the new composition.** In `04-template-detail.png` and its AXL pair, Start is the only accent-filled command. The large navigation title is chrome; Edit is a native toolbar word on glass, not a second filled content action. Exercise names are neutral Headline. A superset's selected amber chip is membership state, excluded by the rule (`TemplateDetailView.swift:90`). On Start, the existing Start Empty Workout capsule still leads and the template tile is neutral. No newly introduced competitor. The active screen's existing Add Exercise/Skip treatments predate this boundary.

2. **Placement — pass.** Title → bare family strip → exercise list, with Start at the thumb above the tab bar, matches the ticket. `TemplateDetailView.swift:54` uses a bottom safe-area inset; the capsule stays there in both scrolled and unscrolled AXL captures. The list is the principal content block; there is no second hero at the top. Default shows all five fixture exercises. AXL preserves order and exposes the remaining rows by scrolling.

3. **Grouping — pass.** `TemplateDetailView.swift:27` has a List, a clear family row and one card-coloured exercise section with separators (`:37–43`). Individual rows have no card wrapper. The template tile is a structured item: families, name, exercises. The little family backplates and membership chip are semantic icons/state, not nested content cards. **There is no spacer section at this HEAD**, contrary to the prompt; no spacer-section tell can be attributed to this code. Clearance is provided by the inset and its padding/gradient (`:54–69`), and the final AXL capture visibly clears the capsule. Judgement: the default gap between the strip and list could be tighter, but it does not violate the recorded composition.

4. **Figures — pass/not applicable to the new detail.** The ticket identifies no standalone glanceable metric on this screen. The sets/reps sentence is supporting metadata, and the exercise count is capsule support, not a stat tile. There is no `hero` figure on the detail and no new competing figure. The existing active workout's time remains the large figure; icon removal did not change its type.

5. **Type — pass.** Search of Start and the family component found no `.system(size:)`, custom font family or Light/Thin weight. Names use `Theme.cardTitle`; captions use `.caption`; capsule title uses bold `.body`, subtitle medium `.caption`; icons use semantic caption/title3 styles. `HeroCapsuleLabel`'s rendering is unchanged by making the type internal (`StartWorkoutView.swift:284`).

6. **Colour — pass for changed content.** Family colours retain their muscle meaning; the selected superset chip uses the same solid amber/OnAccent treatment as the workout. No essential tertiary text was introduced on Elevated or Fill. From REFERENCE: primary/Card 15.6:1, secondary/Card 8.8:1, tertiary/Background 5.4:1, OnAccent/amber 10.7:1. The capsule's 80%-opaque OnAccent subtitle composites to approximately `#44321C` on amber: **6.98:1**, above minimum and approximately the 7:1 target. Family measurements are below. Native Edit tint is chrome consistent with the ticket's explicitly demoted toolbar action; it is not another amber-filled command.

7. **Controls — pass by inspection.** Detail has one prominent button and no destructive primary. The shared capsule has a 56 pt minimum height (`StartWorkoutView.swift:331`), larger at AXL; tile buttons are at least 118 pt before padding (`:359`); Edit uses the native toolbar control. Buttons use the system `.plain` style, which retains system press feedback; no custom style discards `isPressed`. The gym Menu remains neutral and displays its value, unit and disclosure; equal grid columns remain equal options. No new custom small hit target.

8. **Lists, sheets, tabs — pass by inspection.** Existing tile Edit/Delete context menu remains (`StartWorkoutView.swift:76`); the icon-removal hunks remove no swipe/context actions. Detail Edit presents the same staged `TemplateEditorSheet`, with Cancel and Save (`TemplateEditorSheet.swift:59–65`). The detail itself is pushed. Tabs still navigate. `startEmptyWorkout` remains at `StartWorkoutView.swift:165`. Tile tap changing from start to navigation is the user's explicit request, not an accidental behavior change. Start-flow checks are below.

9. **Dynamic Type — partial; P2 above.** Default and AXL use exactly the same Full Session fixture and no-gym state (`RedesignScreenshotUITests.swift:101`, `:129`). All five names and captions are whole across the detail's initial/scrolled captures. The Start-screen capsule wraps “Start Empty Workout”; the detail capsule retains Start over “5 exercises.” Scaled icons stay inside their backplates. Code switches the chip/name layout to VStack at accessibility sizes (`TemplateDetailView.swift:84–90`) and uses `WrapLayout` plus `@ScaledMetric` (`MuscleGroupStyle.swift:33`, `:66`). Those mechanisms look correct, but the supplied three-family/no-superset pictures do not exercise them. No claim about sizes above AccessibilityL.

10. **Motion — pass by inspection.** Detail adds no custom animation; its capsule has `live: false`. The reused live capsule actually gates the animation with `reduceMotion ? nil : …` and forces the live dot's opacity to 1 (`StartWorkoutView.swift:312–314`). This is an effective setting, not an unused environment read. No new decorative motion or timer path.

11. **Copy/identifiers — partial; P2 above.** `git grep -n templateTile 55d6562 -- WorkoutTrackerUITests` returns only two existence assertions at old `RedesignScreenshotUITests.swift:117,142`, no taps. The test diff adds tile taps and detail assertions without deleting old queries. “Start” is proven at `e608fa6^:WorkoutTracker/Features/Start/StartWorkoutView.swift:411`: `Button("Start", action: start)`. “Missing exercise” already exists in the editor, and “Superset position …” already exists in the workout. `startTemplate`, `editTemplate`, `templateFamilies` and `templateExercise.<name>` exist at detail lines 70, 51, 34 and 104. **`templateDetail` does not exist at HEAD**: lines 76–77 explain that the screen-level identifier was removed because it overrode the inset button identifier. No existing test queried it; this is a prompt/implementation discrepancy, not an identifier-preservation regression. The newly composed row caption remains the pending copy decision.

12. **Record — partial.** The committed ticket has the user's words, job/state sentences, relative-size wireframe and all ten tells answered. The unchanged live Start state is inherited from ticket 10. Both sizes and the scrolled detail are supplied, but the evidence gaps in item 9 remain. At pinned HEAD, gate suites are named without counts (`issues/11-icons-and-template-detail.md:95–100`). During this review, the author appended an uncommitted Verification section (`:102–124`): unit 715/715; CoreLoop 9/9, HeartRate 5/5, ExercisePreset 2/2, Barbell 2/2, CodexScreenshot 3/3; initial capture failures followed by successful retakes. Those are author-reported results, not independently rerun results or part of `main..HEAD`. No completed full-suite result is recorded there. Preserve the verification section with the review record and correct the wrapping claim when supplying the missing captures. Mockup skipping is explicitly disclosed in the ticket; this review assesses the requested composition rather than inventing a different one.

**Measured family pairs**

Composited each family foreground at 14% over the actual underlying surface, then calculated WCAG relative-luminance contrast against the opaque foreground. Ratios use unrounded channels; hex values below are rounded for display. Source: `MuscleGroupStyle.swift:13–18,51`; tile surface is Card, detail strip surface is Background.

| Family / foreground | Tile composite on Card | Ratio | Detail composite on Background | Ratio |
| --- | --- | --- | --- | --- |
| Chest `#F4939C` | `#362C32` | 6.09:1 | `#2C2024` | 7.12:1 |
| Back `#8ABCE5` | `#27323C` | 6.50:1 | `#1D262E` | 7.64:1 |
| Shoulders `#E6CF88` | `#34342F` | 8.11:1 | `#2A2821` | 9.58:1 |
| Arms `#C1A9EB` | `#2F2F3D` | 6.37:1 | `#24232F` | 7.48:1 |
| Legs `#A5CF9A` | `#2B3432` | 7.30:1 | `#212823` | 8.61:1 |

All exceed 4.5:1. Existing 12%-tint chips on Card also meet minimum: kg 7.51:1, lb 7.74:1, mixed 6.22:1, secondary 6.94:1, amber 7.70:1. The **selected superset** chip is solid amber with OnAccent, not one of these translucent pairs: 10.7:1 by REFERENCE.

**Start flow, model lifetime and tests**

`WorkoutStartFlow.swift:43–60` preserves the already-in-progress title, Resume/Finish/Cancel actions, completed-set consequence, drift message and Keep Current Workout cancellation. The moved methods match the base except for `selectedGym` becoming the passed `gym` and the request trigger. Ordinary replacement banks `heartRateCoordinator.end(active)` at `:85` **before** template/session start at `:89–92`. Drift replacement banks `end(workout)` at `:129` **before** `TemplateDriftService.resolve` at `:130`, whose finish remains at `TemplateDrift.swift:205`. Both codex-review 05/05b fixes survive. Cancel invokes no finish/start/banking; Resume opens the existing workout. Runtime presentation of the dialogs on the pushed screen was not exercised by the supplied screenshot tests, which stop before tapping Start.

The modifiers have separate inputs: Start's `$startRequest` (`StartWorkoutView.swift:128`) versus detail's `$request` (`TemplateDetailView.swift:72`). A detail tap changes only the latter (`:55`). The observer clears that request and ignores nil (`WorkoutStartFlow.swift:61–64`); there is no forwarding into the parent request. Therefore two modifier instances do not imply two starts for one tap. On success/Resume the detail callback clears `viewingTemplate` before opening the workout (`StartWorkoutView.swift:132–135`).

The proposed **tile-menu deletion while its detail remains pushed is not reachable through the ordinary UI**: the menu lives on the covered Start screen, and the detail editor has no template-delete action. Returning Back ends that optional destination before the menu becomes available. I found no concrete navigation-binding defect on that route. The `isDeleted` checks at detail lines 24/47 only guard rendering; they do not disable Edit/Start or clear the selected model. Judgement: if deletion from another context/device must be supported while the detail remains open, explicitly resolve/dismiss the selected template and guard its actions; the present guard alone does not establish that broader safety. This is not a demonstrated same-stack regression.

Pure logic matches the requested vocabulary: eleven seeded groups map to five ordered, deduplicated families; Core/Neck/Full Body and unknown/nil values produce none. The new tests cover these cases. `Supersets.memberLabels` uses contiguous non-nil runs, ignores singleton runs and restarts A/B after interruptions, matching the workout rule; beyond Z it shares the existing numeric fallback. Detail uses `editableTargets`, matching the set-slot floor used when starting a template. Independent standards/spec checks found no additional source defect.
