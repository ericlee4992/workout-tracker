# Round 1 (T6) — machine deletion ticket 01

Reviewed `cc4aaa5..d35e725` (`delete-machines`, one commit) by inspection against `work-record/machine-deletion/issues/01-delete-machines.md`. No source changes, `xcodebuild`, or `simctl`. Test results recorded in the ticket were not independently rerun.

- **P3 — The context-menu item is not shared as specified.** `WorkoutTracker/Features/ActiveWorkout/AddByMachineSheet.swift:49` and `WorkoutTracker/Features/Gyms/GymsView.swift:268` separately define “Delete Machine…”. Ticket lines 29–30 require one shared component carrying “the swipe, the menu item and the dialog, so both screens cannot drift.” `MachineDeletion.swift` shares only the swipe and alert. Extract the menu button into that component and use it inside both existing menus. Current behavior agrees; this is a partial spec implementation, not an observed deletion failure.

Coverage limitations: `WorkoutTrackerUITests/MachineDeletionUITests.swift:67` starts an empty workout and never logs an entry before deleting. The lifecycle fixture finishes its workout at `WorkoutTrackerTests/EquipmentLifecycleTests.swift:54`. Neither test pins the explicit current-workout screen-retention requirement. Log a completed set, delete its machine, verify the entry and values remain, then finish and verify History. The restore test at `WorkoutTrackerTests/EquipmentLifecycleTests.swift:162` checks UUID, model, labels and the live entry relationship, but leaves both defaults nil and checks the snapshot ID only before restore. Populate `defaultPresetID` and `defaultUnit` and assert those plus snapshot fields after the round trip. These are coverage gaps, not confirmed implementation defects.

The remaining risk checks are clear by inspection. Both Delete routes only call archival; archive/restore change `archived` and save, preserving the row, identity, model, label, defaults, relationships and snapshots. Active-workout and History lists enumerate workout entries without filtering archived machines. Restore can produce duplicate display labels, already permitted by machine creation; UUID identities remain separate, with no overwrite or history merge. Archived gyms cannot reach the deleted-machine list through the active-only Gyms navigation, and still offer no active machines. The sheet's session and lifecycle use the same environment context, and its computed list reads observed archival state. Alert actions capture the presented machine, dismissal clears pending, and swipe/context-menu actions only request confirmation; full swipe is disabled. No reachable wrong-machine or stale-chooser path was found. The alert Cancel lookup at UI-test line 33 is global but has no competing Cancel in that tested Gyms state; line 82 intentionally dismisses the sheet. Scoping the former to `app.alerts` would make the intent explicit.

Standards: clear. Spec: one P3 finding; coverage limitations above. No confirmed user-facing defect.
