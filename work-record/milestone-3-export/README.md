# Milestone 3 — CSV / JSON export

The spec is `spec.md`; tickets are `issues/01..03`. Three tickets, strictly sequential
(01 → 02 → 03) — the whole milestone is one feature, split only so the pure core lands and is
tested before any UI depends on it.

Why this is next: the user's training history exists on exactly one phone with no sync and no
backup (`docs/STATE.md`). Everything else in the backlog is an improvement; this is insurance.

## Status legend

Each ticket carries `Status: ready-for-agent | claimed | resolved` per `docs/agents/issue-tracker.md`.
