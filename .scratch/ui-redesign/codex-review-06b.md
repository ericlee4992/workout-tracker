Round 2 (T6), ticket 06 — **not clear: one new P3**. Reviewed `ba090f6...ee5b17d` on `ui-redesign-06`, the ticket response, and the supplied AccessibilityL and calendar screenshots. Inspection only; no `xcodebuild`, `simctl`, or source edits.

- **P3 — External calendar rings overlap on narrower widths.** `WorkoutTracker/Features/History/HistoryCalendarSheet.swift:149`: `.padding(-4)` paints a 48-point ring around a 40-point cell without reserving additional layout space. The seven columns explicitly have **zero horizontal spacing** (`:15`); the 8 points at `:75` separate rows. With the outer horizontal padding and card inset, a 375-point viewport gives each column `(375 − 64) / 7 = 44.43` points, so adjacent marked-future rings overlap by **3.57 points**. At 393 points they still overlap by **1 point**; the current geometry requires at least 400 points of viewport width for non-overlapping rings. A marked-today ring beside tomorrow's marked-future ring has the same problem. Adjust the horizontal insets or ring geometry so the painted diameter fits the actual column width. This is a geometry finding, not an observed simulator capture. The vertical spacing is sufficient (40 + 8 = 48, rings tangent), and the card padding contains the outer edges; horizontal overlap is the remaining issue. Apple's [overlay documentation](https://developer.apple.com/documentation/swiftui/view/overlay(alignment:content:)) confirms that the underlying view dominates layout.

The original two P2s and P3 are closed: both chart marks use `.monotone`, with the plan deviation documented; marked-future numerals now contrast **15.59:1** against the card and their accent ring **9.83:1**; today/marked-today secondary rings contrast **8.79:1** against the card. The negative padding puts the 2-point stroke outside the disc with a 2-point clear gap, removing the amber adjacency problem. Filled-day numerals retain **10.69:1** contrast. These figures come from the actual assets. The first detail Section now has `Theme.card` and hairline separators. The two AccessibilityL captures show a complete two-digit day, wrapped workout title, stacked equipment/chip, and unbroken load-type label. The `AnyLayout`s change arrangement inside the existing controls; the row remains one Button, its navigation/swipe behavior and IDs remain unchanged, and the subtitle Menu retains its actions/ID. No new snapshot, copy, or positional-test regression found. The new screenshot test requests AccessibilityL explicitly, and the corrected initial gate counts match the test methods. The captures use “No equipment”; they do not prove every long equipment label or larger accessibility size, but no additional defect is established by inspection.
