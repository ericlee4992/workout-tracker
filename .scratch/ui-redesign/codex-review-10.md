# Ticket 10 — T6 round 1

**Not clear: four medium findings and one low finding.** Reviewed `main...HEAD` on `ui-redesign-10-start`: `d4923746354d1e7d5af29ce243039d42f311fe1a` → `e608fa6df7e1bba96feea4afca576e11239f7848`. Read SKILL.md, REVIEW.md, REFERENCE.md, the ticket, and Main/MainLive canvas sources; visually inspected all three supplied PNGs. No source files changed; no xcodebuild or simctl commands run. Reported test results are not independently rerun. REVIEW.md contains 12 numbered checks; item 13 below is its advisory paragraph.

**Medium / P2 — The gym picker was not visually demoted; its supporting text also fails contrast.** `WorkoutTracker/Features/Start/StartWorkoutView.swift:333` and `:338`. All three captures show an amber gym title (`Iron Temple` or `No gym`), contrary to the final composition's explicit “no accent title” (ticket:87–89). The semantic `.primary`/`.secondary` styles inside this Menu inherit the amber treatment; they do not render as the intended neutral text tokens. In `04-start-templates.png`, solid subtitle pixels are `#8B6840` on `#171B21`: **3.42:1**, below 4.5:1. Use explicit neutral text tokens for the value/support/disclosure and recapture. This picker code predates the diff, but the requested second-pass demotion and whole-screen design gate leave it unresolved; this is not attributed as a newly introduced regression. The small pin tile is a separate question, answered under item 1.

**Medium / P2 — The five-exercise summary is visibly truncated at both gated sizes.** `WorkoutTracker/Features/Start/StartWorkoutView.swift:417`. The default tile ends `Assisted P…`; AXL ends `Back…`, omitting Belt Squat. The name is whole, but the exercise string is not. Retaining the former two-line cap in the narrower grid does not satisfy SKILL step 6 / REVIEW item 9 or the ticket's “every string whole” claim. Let the required summary expand, or explicitly obtain and record an exception for an abbreviated preview; the accepted grid composition alone does not resolve the contradictory claim. Verify the complete five-exercise string in replacement captures.

**Medium / P2 — Scaling the small muscle tile changes its font category and makes symbols outgrow their backgrounds.** `WorkoutTracker/Features/Design/MuscleGroupStyle.swift:46`; also `WorkoutTracker/Features/Start/StartWorkoutView.swift:368`. `side` is already dynamically scaled, so the 24-point variant crosses the 32-point threshold and switches from scaled Caption to scaled Title 3. In `04-start-axl.png`, the Core symbol extends outside its small rounded background toward the adjacent Chest symbol; the Chest figure also exceeds its background. Choose the semantic font from the base variant, then scale its container consistently. The hero has the related visible problem: its Body symbol grows while its ink disc stays 40 points, leaving the figure flush with/over the disc boundary at AXL. Scale that disc with the glyph too. This is visible evidence, not an inference that `@ScaledMetric` is absent.

**Medium / P2 — The capture record does not cover the whole AXL layout or the changed live composition.** `WorkoutTrackerUITests/RedesignScreenshotUITests.swift:143` and `:95`. The AXL test takes only its initial viewport: New Template is obscured by the tab bar and the machine-resolution sentence is below the capture. SKILL step 6 explicitly requires scrolling until every named block is on record. There is also no AXL live capture for the new two-line Resume capsule. Add a scrolled idle capture and a same-fixture live default/AXL pair. The idle captures do show the same visible No gym / Full Session state, but their setup is not identical: the default test creates Iron Temple at `:102` without selecting it, while AXL creates no gym. Align the setup or correct the default test's “gym chosen” claim and assert the intended value.

**Low / P3 — The gate counts cannot be reconciled.** `.scratch/ui-redesign/issues/10-start-second-pass.md:115`. The written counts total 25 including the three Start captures, not the claimed 23. `ExercisePresetUITests.swift:25` and `:71` contain two test methods, not three; the currently named groups plus captures total 24 distinct tests. Reconcile the ticket against saved results, distinguishing the successful test set from retries. No claim here that tests failed.

**Checklist — checkable answers to items 1–13**

1. **Bold element — fails the recorded demotion.** The capsule is the only accent-filled command and sits above neutral template tiles. I accept the **pin tile as the subdued picker-context/state treatment**, not a competing command: its 10% fill is confined to the 44-point leading icon inside a neutral value-and-disclosure control (`StartWorkoutView.swift:327–344`); it does not fill the picker or use the largest content type. Its tint is unconditional, including No gym, so it marks current location context rather than a selected/unselected color transition. That acceptance does **not** excuse the amber content title: the final record explicitly demotes that title, and SKILL's dominance rule prohibits a competing accent content title. The selected Workout tab is exempt state; the gear is unchanged native navigation chrome.

2. **Placement — passes at default size; AXL coverage incomplete.** The captures follow title → gym → capsule → Templates/grid → helper sentence, as the user's final Main structure specifies. The capsule is top-leading beneath the gym, not pinned at the thumb. Default captures contain the named blocks. MainLive still shows the older above-gym Resume plus secondary Start; ticket:77–89 supersedes that older canvas state. AXL's lower blocks require the missing scroll capture.

3. **Grouping — accepted composition exception.** The gym groups location/value/unit/disclosure; template cards group icons/name/exercises. The capsule is the command itself, not a card around another button. Small icon backgrounds are markers, not nested content cards. New Template as a filled tile and the two-column collection are explicitly selected in ticket:68–72 and Main.dc.html. I accept those exceptions to the generic “more than three like items are lists” rule, including when more templates are added; reverting to rows would undo the user's choice. Peer template/New radii use `Theme.Radius.card` (`StartWorkoutView.swift:95`, `:421`).

4. **Figures — passes / no nominated glanceable statistic.** This screen's bold element is a command. Its inline exercise count is supporting subtitle metadata, not a nominated stat/hero figure. No hero-weight content figure is duplicated; subtitle remains smaller than title (`StartWorkoutView.swift:382–384`).

5. **Type — text passes; one literal exception.** New text uses Body, Caption, Headline via `Theme.cardTitle`, and Subheadline; no Light weight or alternative family. The grep is **not empty**: `StartWorkoutView.swift:374` uses `.system(size: 9)` for the hidden pulse dot. This is a decorative marker rather than text, but it does not literally meet the all-styles wording. Record that narrow exception or draw a 9-point circle as geometry. The substantive scaling defect is the separate P2 above.

6. **Colour — fails on the gym support text.** Reference-table pairs used elsewhere pass: template secondary/Card 8.8:1, helper tertiary/Background 5.4:1, New Template primary/Fill 11.7:1, capsule OnAccent/accent 10.7:1. Measured sRGB alpha-composited pairs: 80%-opacity capsule subtitle/accent **6.98:1**; amber pin/10%-amber-on-Card **8.06:1**; lb/12%-lb-on-Card **7.74:1**, kg equivalent **7.51:1**. Visible muscle glyphs on their 14%-tinted Card backgrounds: Core **6.86**, Chest **6.09**, Back **6.50**, Quads **7.30:1**. Their hues retain muscle meaning. These calculations use asset/token RGB values with foreground alpha composed before luminance; the failing **3.42:1** gym pair uses solid rendered PNG pixels. No essential `Theme.tertiary` text is placed on Elevated/Fill. The rendered amber title also violates its recorded demotion.

7. **Controls — structural checks pass; pressed rendering unverified.** One prominent filled command, 56-point capsule minimum, 44-point gym icon plus padding, and template/New tiles with 118-point minimum content height plus padding. Grid columns are equal-width; destructive Delete stays in the menu, never primary. The picker shows value and disclosure on a neutral card. These use native `.plain`, not a custom ButtonStyle missing a press implementation. Apple documents that [plain style can apply pressed-state effects](https://developer.apple.com/documentation/swiftui/primitivebuttonstyle/plain); a still capture cannot establish their appearance. I do not manufacture a missing-press-state finding from `.plain` alone.

8. **Lists, sheets, tab bar — accepted.** Yes, I accept losing swipe-to-delete for this explicitly accepted grid. `StartWorkoutView.swift:75–83` retains Edit… and destructive Delete on each template's context menu, calling the same editor/delete service. A grid has no per-template List rows on which to retain swipe. Reduced discoverability is the accepted interaction tradeoff. The template sheet's staged Cancel/Save behavior is unchanged, as are confirmation-dialog choices. RootView's four tabs still only navigate; symbols remain untouched.

9. **Dynamic Type — fails, but not because two columns remain.** `StartWorkoutView.swift:66–67` explicitly changes to **one column** for accessibility sizes, and the PNG confirms it. Five icons and the whole Full Session name fit within the large card's overall width; the two-line summary still truncates, and individual symbols exceed their backgrounds. `WrapLayout` is present and the capsule title wraps intact. The gym still uses its inherited horizontal layout with unit/disclosure trailing (`:326–344`), rather than stacking accessories as the rubric requests; its fixed icon background also fails to grow with its glyph. Hierarchy order is retained, but lower-block and live-AXL evidence are missing. See the P2 findings.

10. **Motion — source check passes.** The only new repeating animation is the live dot. `HeroCapsuleLabel` reads Reduce Motion at `:360` and actually gates `.symbolEffect(.pulse, … isActive: !reduceMotion)` at `:376`; the dot stays present but still when enabled. `:378` hides it from accessibility. No other explicit screen animation was introduced. Runtime Reduce Motion behavior was not exercised in this review.

11. **Copy, identifiers, and accessibility — accepted intended changes; no hidden button dependency found.** Idle `startEmptyWorkout` and live `resumeWorkout` remain on their corresponding Button branches (`:157–173`). Removing Start while live is expressly authorized by ticket:77–83. The old per-template Start caption is replaced by the selected whole-tile interaction; existing exercise/helper strings are retained, and `templateTile.<name>` is an additive identifier. No UI-test caller requires the idle button during a live workout; CoreLoop's minimize/resume case explicitly queries `resumeWorkout` (`CoreLoopUITests.swift:477–498`).

   Repository-wide entry-point audit: no `onOpenURL`, custom workout URL handler, widget `Link`/`widgetURL`, or app-intent start route exists. `WorkoutTrackerWidget/WorkoutActivityView.swift:88` supplies presentation only; a Live Activity opening the app does not query a button identifier. `RootView.swift:93–95` recovers directly through `WorkoutSession.resumableWorkout()`, and an already-minimized screen exposes Resume. The finish sheet has **no “start another” action**: Done dismisses (`WorkoutFinishedSheet.swift:141`, `RootView.swift:82`), View in History navigates (`RootView.swift:76–80`), and finishing has already ended/discarded the session (`ActiveWorkoutView.swift:551–589`). Template-start-while-live still routes through `startTapped` and the existing replacement dialog (`StartWorkoutView.swift:127–144`, `:187–193`). Nothing found depends on the removed live-state idle button.

   For the live capsule, the intended combined content is **“Resume workout, Iron Temple · 0 exercises”**, with the Button trait, or the unchanged “In progress · N exercises” fallback. `.combine` retains the title/subtitle properties, and the pulse is excluded. Apple's [combine documentation](https://developer.apple.com/documentation/swiftui/accessibilitychildbehavior/combine) allows transformations when merging non-hidden children: this is not proof of the exact spoken label. The figure and chevron are not explicitly hidden (`:365`, `:387`), so symbol-name inclusion remains unverified without an accessibility-tree/VoiceOver check. No claim that a PNG verifies the final spoken output.

12. **Record — incomplete/inaccurate.** Job/state sentences, alternatives, user decisions, tells, named gates and three PNGs exist. The final prose supersedes older wireframes/MainLive. However, “no accent title,” “every string whole,” complete AXL coverage, “gym chosen,” and gate counts conflict with the evidence above. Update the record after fixes and retake captures for every pixel-moving fix; the supplied STATE does not independently establish this branch's full-suite result.

13. **Advisory / judgement.** The smaller capsule and structured template tiles make the primary action easier to distinguish than the old full-width slab. The default empty/live state still has substantial free space, which is reasonable for a personal logger with no templates; filling that space is not itself a goal. I would keep the chosen composition and fix the specific hierarchy, legibility and scaling failures rather than reopen the user's structural choice.

The parallel source reviews found no additional independently confirmed behavior defect. Their checks are incorporated above; the severity findings combine the rubric, final spec, and direct visual evidence.
