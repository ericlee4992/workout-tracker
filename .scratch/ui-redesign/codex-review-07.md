# Ticket 07 — Codex review, round 1 (T6)

Reviewed `main..HEAD` on `ui-redesign-07`: `bf4d919` → `b0e924f`. Inspected the source diff, ticket, plan, supporting navigation/design/domain code, gate tests, and all four supplied screenshots. No source changes; no `xcodebuild` or `simctl` runs. Runtime results below are the ticket's recorded verification, not fresh verification.

- **P2 — Preserve the full model name at accessibility sizes — `WorkoutTracker/Features/Gyms/GymsView.swift:313`.** The new `.lineLimit(1).minimumScaleFactor(0.85)` removes the previous caption's ability to wrap. The fixed 40 pt tile, card padding, chip padding and optional unit badge further constrain the name. The supplied default-size screenshot fits “Life Fitness Insignia Series Chest Press,” but increasing Dynamic Type will truncate long names, hiding the distinguishing model suffix. This conflicts with the plan's typography requirement “so Dynamic Type survives.” **I would ship the chip at standard sizes and a wrapping caption at accessibility sizes**, with sufficient width below the title/tile as needed; record that limited exception to “the model as a chip.” The plan also requires “one AXL screenshot per ticket”; none of the four supplied captures or ticket verification records covers AXL. Verify the adjusted layout with the long model name and a unit badge when the simulator is available. This is a source-level layout finding, not a claimed observed simulator failure.

Other requested checks:

- **Navigation:** `GymsView.swift:18` appends the same gym UUID accepted by the unchanged destination at line 39. `RootView.swift:103` only selects the Gyms tab for `PROTO_SCREEN=gyms`; Start's gym picker selects/remembers a workout gym and does not push gym detail. The only `GymDetailView` construction is this destination; no sheet or URL handler bypasses the row to push it. All repository `gymRow.*` test queries use any-element identifier lookup, not a link-only query. No navigation regression found.
- **Swipe/accessibility/ids:** `GymsView.swift:332` uses `.contain` appropriately: it establishes a container while retaining child elements, including the model `Text`. Apple's [accessibility child behavior documentation](https://developer.apple.com/documentation/swiftui/accessibilitychildbehavior) supports that distinction from combining children. The swipe modifier still belongs to the List row; clear row paint does not remove it. Horizontal row insets are zero, and padding is inside the card, so the 40 pt tile is no longer the intended swipe target. The recorded post-fix deletion gates support the correction. Context-menu actions are unchanged. `machineRow.<label>` (line 333), `deleteMachine.<label>` (`MachineDeletion.swift:23`), and `restoreMachine.<label>` (line 81) are unchanged.
- **Count cost:** `GymsView.swift:69` makes no explicit fetch, but `EquipmentLifecycle.swift:25` filters and sorts the relationship on every evaluation: O(m + a log a) for m total/a active machines, plus temporary arrays. The gym query does not explicitly prefetch machines; “already loaded” is not guaranteed. Relationship access can require storage access, as Apple's [SwiftData prefetch documentation](https://developer.apple.com/documentation/swiftdata/fetchdescriptor/relationshipkeypathsforprefetching) explains. I would accept this for the app's small personal gym inventory, but would not certify a many-gym list as cheap without measurement. A domain count that skips sorting is a reasonable later optimization; no measured regression warrants a separate finding here.
- **Button hierarchy/copy:** Secondary Add Gym beside existing gyms, primary when empty, is reasonable; the plan explicitly mandates primary Add Machine and leaves Add Gym's prominence open. The only newly added visible `Text` content is the permitted number. `EmptyState` still emits “No machines yet” as a static text, and “Nothing deleted” remains unchanged. “N machines” is technically new accessibility wording, but supplies the meaning of the explicitly requested number/symbol chip; it is not new visible explanatory copy or a copy-policy violation. The ticket's “No new copy” would be more precise as “No new visible copy.”
- **Four gates:** Deletion alert/swipe/restore logic and scan/Ask-AI strings and ids are unchanged; the scan sheet itself is untouched. `.contain` preserves the scan test's full-model static-text lookup and long-press target. One correction to the prompt: `DumbbellCounterpartUITests` reads `exerciseOption.Bench Press` and `staticTexts["Bench Press"]`, not `machineRow.*`; that path is outside this diff. The ticket records Scan 2/2 and Ask AI 7/7 before the container fix, then Deletion 2/2 and DumbbellCounterpart 1/1 after it; it does not establish a fresh post-fix run of all four classes.

Standards: clear, 0 findings. Spec: 1 finding, P2 model-name truncation at accessibility sizes. Not clear until that finding is addressed.
