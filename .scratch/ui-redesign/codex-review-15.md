# Ticket 15 — Round 1 (T6)

Reviewed `a350944bf2bc585a7fd29a90c9ff3bd0c149c415...b324a4da964f4269c487f10d9a4614356d78d505` on `templates-15-delete-in-detail`. Two P3 documentation findings; no demonstrated functional regression.

- **P3 — Qualify the root-cause claim.** `WorkoutTracker/Features/Start/StartWorkoutView.swift:77` and `.scratch/ui-redesign/issues/15-delete-template-in-detail.md:11` state row-level context-menu attribution as an established cause. The grid really is one List row, containing multiple tile menus in the base revision, so menu interaction at that shared row is a plausible leading explanation. However, the ticket explicitly says it was neither reproduced nor investigated further. [Apple documents the overload used here as attaching a menu to a view](https://developer.apple.com/documentation/swiftui/view/contextmenu(menuitems:)); that does not establish the claimed arbitration between multiple menus within a List row. Label this a suspected cause. There is no evidence for the alternatives: `ForEach(templates)` uses model identity, not indices or names (`StartWorkoutView.swift:69`; `Domain/Models.swift:206`); each closure receives its own `template` parameter and the old helper deletes that exact object, with no shared mutable loop variable or selection lookup. Capturing a class instance does not make it refer to the next instance. Lazy grid reuse alone is not evidence of misidentification. Removing the menus eliminates this interaction regardless of its precise framework mechanism.

- **P3 — Complete the small design record.** `.scratch/ui-redesign/issues/15-delete-template-in-detail.md:18` lacks the job/state sentence, updated wireframe and tells assessment required by `.claude/skills/ios-design/REVIEW.md:40` (item 12). Reference ticket 11's inherited composition and record the new last row and confirmation state; a brief amendment is enough. The small scope can skip mockups, but the skill does not waive these record requirements.

Deletion lifecycle: `TemplateDetailView.swift:134` calls a service that deletes **and saves** (`Domain/WorkoutTemplates.swift:108`) before `dismiss()`. There is no suspension between these operations, but dismissal does not establish that the outgoing view can never be evaluated again. The guards at `TemplateDetailView.swift:29` and `:71` are not a post-save guarantee: a standalone macOS SwiftData parent/cascade-child probe showed both models changing from `isDeleted == true` before save to `false`, with `modelContext == nil`, after save. Reading their already materialized name/items still succeeded. Thus a post-save evaluation can enter the normal body; this review did not reproduce a fault or establish an iOS crash. Advisory hardening: use view-owned deletion state to stop model-backed rendering, or clear the navigation selection before deletion, as in [Apple's deletion sample](https://developer.apple.com/documentation/swiftdata/deleting-persistent-data-from-your-app). Do not describe the existing guards as proving safety.

LIVE workout/source deletion is clear. Only template items cascade (`Domain/Models.swift:210`); workouts retain scalar source ID/name snapshots (`:295`). `TemplateDriftService.sourceTemplate` returns nil for a missing UUID (`Domain/TemplateDrift.swift:122`), and both finishing (`Features/ActiveWorkout/ActiveWorkoutView.swift:537`) and replacing (`Features/Start/WorkoutStartFlow.swift:108`) skip the drift prompt and continue normally. An already-presented drift dialog prevents navigating to this delete button; this change introduces no reachable stale-dialog path.

Design and copy are clear apart from the record finding. The red native text button is last, has a 44-point minimum, and leaves the pinned Start capsule dominant (`TemplateDetailView.swift:52`). The matching Full Session default/AccessibilityL captures show the complete label, including after scrolling. Items 7 and 8 pass: no destructive primary, and editor/start-flow sheet and commit semantics are unchanged. Judgment: History's menu item (`Features/History/WorkoutDetailView.swift:368`) is consistent enough in destructive styling and confirmation; the explicit request for a visible template button justifies different placement. No History redesign is needed. “Delete Template…” names the requested action; the consequence sentence is useful under `docs/SPEC.md:71` because keeping past workouts after deleting their source plan is non-obvious. It is one short consequence line, not screen instructions, and needs no additional permission.

Tests: the two new UI tests cover menu absence, confirmation, deleting Second while preserving Whole Body, popping, and Cancel preserving the detail/template (`WorkoutTrackerUITests/TemplateDetailUITests.swift:21`, `:53`). The conditional tap appropriately tolerates long-press release opening the detail. Existing `WorkoutTemplateTests.swift:15` covers persisted deletion; `HistoryRenderingTests.swift:255` covers history provenance after deletion. Optional additional regression coverage is deleting the source of a LIVE workout, then finishing/replacing it. These tests do not diagnose the former phone-only menu bug or prove every navigation-animation lifecycle. The ticket reports 2/2 targeted UI tests; the full suite was running and its result was not independently verified. No xcodebuild or simctl ran, and no source files were modified.

Standards: 1 finding, worst P3. Spec implementation: 0 findings. Diagnostic documentation: 1 finding, P3.
