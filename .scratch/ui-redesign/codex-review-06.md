Round 1 (T6), ticket 06 — **not clear**. Reviewed `433d468...ba090f6` on `ui-redesign-06`, including the four History views, their callers/components, relevant tests, ticket/spec/decisions, and all six supplied screenshots. Inspection only; no `xcodebuild` or `simctl`, no source changes.

1. **P2 — Preserve monotonic interpolation so the chart cannot invent extrema.** `WorkoutTracker/Features/History/ExerciseProgressView.swift:138` and `:146` replace the previous monotone curve with Catmull–Rom. A plateau surrounded by rising observations (for example, successive values 50, 100, 100, 150) can acquire an intervening hump/dip instead of staying flat between the equal observations. The fill reinforces that contour; sharing the same input points does not make the interpolated picture equally faithful. For assistance this can suggest an improvement/reversal that was never logged. Keep `.monotone` on both marks. This implements the ticket's literal Catmull–Rom request, so the correction needs a documented plan deviation to preserve the chart's “no picture more confident than the data” constraint. Apple's [monotone documentation](https://developer.apple.com/documentation/charts/interpolationmethod/monotone) explicitly identifies its monotonicity guarantee.

2. **P2 — Restore a readable, distinguishable marked-future calendar state.** `WorkoutTracker/Features/History/HistoryCalendarSheet.swift:122`: compositing the actual amber asset at 35% over `Theme.card` gives only **2.06:1** contrast for `Theme.tertiary` numerals and **2.31:1** for the disc against the card. This remains an actionable workout day. Previously it retained a separate checkmark; now the low-contrast disc is its only workout marker. Brighten the numeral and give the mark sufficient contrast while retaining a distinguishable future treatment. Also check `:117`: the marked-today ring is only **1.59:1** against its amber interior; a contrasting ring would communicate today more reliably than the weight change alone. These are asset-based calculations, not simulator measurements. Both text and meaningful state cues fall below even the 3:1 accessibility benchmark here. See W3C's [text contrast](https://www.w3.org/WAI/WCAG21/Understanding/contrast-minimum.html) and [state-indicator contrast](https://www.w3.org/WAI/WCAG22/Understanding/non-text-contrast.html) guidance.

3. **P3 — Apply the card background to the first detail section.** `WorkoutTracker/Features/History/WorkoutDetailView.swift:39`: the Name/gym/duration section never receives `.listRowBackground(Theme.card)`. Hiding the List's scroll background at `:234` does not replace individual row backgrounds. Both detail screenshots visibly retain the neutral system-grey card above the graphite exercise cards. This misses the ticket's explicit claim that “the name/gym/duration rows … sit on `Theme.card`.” Add the section/row background treatment here too.

**Required verification still outstanding:** `HistoryView.swift:192` fixes the new day/weekday stack at 52×56 while both fonts scale. `WorkoutDetailView.swift:89` adds fixed icon/chart widths around a wrapping name and the equipment/load-type HStack. An AccessibilityL capture of the History row and detail header is needed, as the plan requires; include a two-digit day and a long equipment label. The supplied default-size captures cannot establish whether text clips, wraps poorly, or overflows at that size. This is an unverified layout risk, not a claim that clipping was observed. No simulator work was performed during this review.

The remaining requested checks are clear by inspection:

- **Entry routes and deletion:** the row button appends the registered `Workout` type to the existing stack. Calendar `onDismiss` still revalidates its pick and sets `path`; `openTarget` still consumes the receipt target on appear/change. Swipe-delete remains attached to the List row and still presents confirmation. Tests query `historyWorkoutRow` across any element type. A native Button remains an accessible activation control; I see no demonstrated regression from losing NavigationLink semantics. VoiceOver traversal and actual swipe interaction were not exercised. `HistoryEditingUITests` deletes through the detail menu, so its green result alone does not verify the row swipe.
- **Calendar:** ordinary marked numerals have **10.69:1** contrast on amber, and amber/card is **9.83:1**. A visible filled disc supplies a shape/luminance cue, so removing the check is acceptable for that state. The unmarked-today `Theme.secondary` ring is justified (**8.79:1** against card), preferable to the 7%-white hairline. All six state branches, interactivity, labels, and IDs survive. VoiceOver distinguishes today and the three marked states; plain and unmarked future both say “Day N”, exactly as before, so they are not six distinct spoken states.
- **Snapshots and editing:** accept the live muscle icon as the explicitly requested, accessibility-hidden decoration. Its **glyph as well as colour** can change; the ticket/comment should describe that accurately. Display strings, historical identity, load type, and chart variation still use snapshots. The Menu retains `historyEntryLoadType`, `removeHistoryExercise`, its actions, and the reclassified mark; `historyEntryChart` remains on the chart button. No HistoryEditing UI test queries those menu controls positionally. The tooltip tests' indexed workout rows retain their ordering.
- **Heart rate:** all four tiles retain independent optional-fact guards; total calories still requires both energy components. Values remain plain, and StatTile combines each label/value with its explicit accessibility text. `historyHeartRateSection` survives. HeartRateSummaryUITests query the section/chart/caption IDs, never a “Heart rate” header. Aggregate-only workouts remain intelligible through tile labels even without a chart/header.
- **Chart and copy:** the area itself adds no observations or forecast, and the assisted axis still says “Assistance (less is better)”; no separate objection to that fill. The manual overlay, selection mapping, 240-point chart height, and row insets are unchanged. X/Y value labels and the y-axis title remain; the custom Y axis omits ticks, not labels. Change sign still comes from the direction-aware domain calculation; the percentage receives `Theme.stat` and amber where green was used. No new explanatory `Text` copy was introduced; existing empty-state descriptions, set strings, and tested IDs remain. Section background modifiers work for the other displayed rows, as the screenshots show.

Verification provenance note: the ticket's per-class counts do not match the current test source (HistoryCalendar has 2 methods, HistoryEditing 2, HeartRateSummary 3, ProgressChartTooltip 4). Reconcile the recorded counts with the result bundle before treating that table as complete coverage; this discrepancy does not itself prove a test failed. Full-suite verification remains outside this inspection.
