# Ticket 08 — Codex round 1 (T6)

Reviewed `main..HEAD` on `ui-redesign-08`: `f2bfa7a..b5c85c0` (diff: `git diff main...HEAD`). Inspection only, including both ticket-08 screenshots and the named gate tests. No source changes, `xcodebuild`, or `simctl`.

1. **P2 — Four-tag rows need a width-based fallback at ordinary text sizes.** `WorkoutTracker/Features/ActiveWorkout/ExercisePickerSheet.swift:121` (layout selection) and `:137` (fixed-size labels). Every non-accessibility size uses an unwrapping horizontal stack. A supported custom exercise with Barbell, Dumbbell, Cable, and **Smith machine** cannot fit the usual iPhone row alongside the 40-point icon, spacing, and the Exercises tab's Custom marker. The chips alone add 80 points of horizontal padding plus inter-chip gaps; their text cannot compress or wrap. Larger non-accessibility sizes worsen this. The supplied default screenshot contains only short tag combinations, and the bundled catalog currently has at most two tags, so neither establishes four-tag fit. Use available-width wrapping or a vertical fallback at ordinary sizes too, preserving complete labels. This is a layout defect, not an objection to one chip per tag.

2. **P2 — Scaling the shared icon overflows Start's template cards.** `WorkoutTracker/Features/Design/MuscleGroupStyle.swift:35`. `TemplateRow` in `WorkoutTracker/Features/Start/StartWorkoutView.swift:385` keeps up to five icons in one horizontal strip, with the Start button beside that strip's column. At AccessibilityL the tile is approximately 70 points, as visible in the supplied exercise capture: five tiles plus four 6-point gaps require roughly 374 points before the button or card padding. The former strip required 224 points. This exceeds ordinary iPhone card width and forces content outside its available bounds. Adapt the template host to wrap the icons/stack the action while retaining the accessible icon scaling. The recaptured History header does not cover this host.

The remaining scoped checks are clear by inspection. No test reads the removed joined subtitle; the body area and tag words remain exposed, and `Text(name)` remains a separate child for DumbbellCounterpart's query. Exact VoiceOver grouping/traversal was not verified; preserved words do not prove identical speech. AXL's vertical chips are an acceptable readability tradeoff (four tags means four equipment chips, with a fifth chip only for a non-default load type). The subdued amber badge differs from a solid primary action, and the shared dictionary lookup/icon rendering presents no actionable performance concern. ExercisesView actually retains an `HStack` with a context menu; plain Button rows are in the pickers. No gesture/action changes accompany the section backgrounds. Leaving toolbar Save/Add/Done alone matches the explicit clarification in `docs/STATE.md:33`; the in-list styles are appropriate. No new literal copy or changes to filter IDs, “Search exercises”, `noExercisesMatch`, `createExerciseFromSearch`, `exerciseOption.*`, or preset controls were introduced. `barCustomApply` retains its validation, `.disabled(!isCustomValid)`, and a style that reads `isEnabled`; BarbellUITests does not directly exercise the custom-bar controls. Targeted passing results are recorded in the ticket; its full-suite acceptance gate remains pending, and STATE's full-suite result belongs to ticket 07.
