# Ticket 09 — Codex round 1 (T6)

Reviewed `main..HEAD`, `f340a756..59fd54a8`, on `ui-redesign-09`. Inspection only; no source changes, `xcodebuild`, or `simctl`. Accepted the documented amber icon/Live Activity deviations. Standards axis: no documented coding-standard violations or actionable smells. Spec/correctness findings, ordered by severity:

1. **P2 — Pair the opaque Live Activity background with explicit content foregrounds.** `WorkoutTrackerWidget/WorkoutActivityView.swift:84` fixes the Lock Screen background to ink, but the exercise and rest text inherit primary foregrounds, and the elapsed timer/set count explicitly use adaptive `.secondary` (`:34`, `:59`). Whenever the host supplies a light appearance, these become dark text against near-black. The app's `.preferredColorScheme(.dark)` does not configure the separate widget extension, and `activitySystemActionForegroundColor` only colors the system action. Set appropriate light primary/secondary content colors for this fixed background, or explicitly provide the matching color scheme within the Lock Screen content. This is an inspection-derived appearance risk, not an observed runtime failure. Apple documents both Lock Screen appearances and requires custom backgrounds to work with them; full opacity itself is supported. [Apple: creating custom Live Activity views](https://developer.apple.com/documentation/activitykit/creating-custom-views-for-live-activities?changes=latest_5).

2. **P2 — The final empty-state sweep is incomplete.** `WorkoutTracker/Features/Exercises/ExercisePresetsSheet.swift:39` still uses plain `Text("No presets yet. Without any, this exercise is logged as one thing.")` with identifier `noPresets`. `WorkoutTracker/Features/ActiveWorkout/PreviousPerformanceSheet.swift:22` and `:108` render five other plain messages from `WorkoutTracker/Domain/PreviousPerformance.swift:19`: “No completed sets on this machine yet.”, “No completed sets with this equipment yet.”, “No other gyms with this model logged yet.”, “No history for this exercise yet.”, and “No eligible records at this layer yet.” Plan ticket 09 (`spec.md:208`) says **every** remaining “No … yet” becomes `EmptyState`. Ticket 08 deliberately retained these native sheets, so documenting an explicit ticket-09 exception is also reasonable, especially for repeated per-layer messages; the current ticket instead calls the three replacements the last remaining texts. Preserve the full copy and identifiers if migrating.

3. **P3 — The outer icon plates float off the shaft.** `scripts/render-app-icon.py:20` draws the shaft only from x=256 to 768 at output scale. The inner plates occupy x=210–297 and 727–814; the outer plates occupy x≈104.75–191.75 and 832.25–919.25. Consequently each 18.25 px gap is background even at shaft height. This is visible in the committed PNG and remains about 1.07 pt at a 60 pt icon. It still reads as a dumbbell, so this is a low-severity geometry/design finding. Extend the shaft beneath both outer plates before drawing the plates, then regenerate the PNG.

4. **P3 — Narrow the Visual design paragraph to what the tree proves.** `docs/SPEC.md:76` says every screen sits on `SurfaceBackground`, but the intentionally retained native Presets/Previous Performance sheets do not apply it. `:81` says muscle symbols appear wherever an exercise is named, although those sheets' navigation titles are plain names. `:85` claims an AccessibilityL capture for each screen; the tree has captures for active workout, finish, Start, Exercises, Gyms/detail and History/detail, but no identifiable AXL Settings, chart, calendar or picker/empty-sheet captures. These are documentation overclaims, not a request to redesign all native forms. The token palette, warmup separation, shared components, wrapping/scaling code, monotone progress chart and named haptic triggers are verifiable. This round verifies copy preservation in `main..HEAD`; it does not establish a byte-for-byte audit of every visible string across all nine tickets.

5. **P3 — Finish the handoff documentation without claiming pending gates passed.** `docs/DECISIONS.md:63` says tickets 01–09 were “each Codex-reviewed to clear”; ticket 09 is currently awaiting this first review, and the 01/02 record describes Claude reviewing the selected Codex implementation. `docs/STATE.md:3` and `:30` are unchanged from main and still say ticket 09 is next. The ticket's `:39` “Full suite: see STATE” points to ticket 08's results, not ticket 09 verification. Update STATE with the actual branch/review/test status and describe independent cross-review accurately; finalize only after the remaining gates.

Inspection checks:

- **Empty list rows:** approximately 220–225 pt of content before List insets (128 pt illustration + 20 pt spacing + headline + 48 pt padding). Substantial for a sheet, especially above MachinePicker's free-weight choices, but finite, scrollable and acceptable by inspection; no demonstrated clipping or unreachable action. A smaller illustration would be a preference, not a correctness fix. `EmptyState.swift:21` hides only the artwork; `:22` remains `Text(title)`, with no accessibility grouping that would replace the title. The three replacement strings and all existing identifiers are byte-identical. The ticket records passing `MachineDeletionUITests` staticTexts queries. The supplied History/Gyms/root screenshots look consistent, but do not show these three changed empty rows.
- **Icon packaging:** confirmed 1024×1024, 8-bit PNG color type 2 (RGB), no alpha and no `tRNS` chunk. Plates mirror around the center; all geometry remains inside the tile (roughly x=105–919, y=287–737), and the shaft is painted first. Inspected the original plus 180 px and 60 px reductions: recognizable at 60 pt. `Contents.json` correctly supplies `filename`, `idiom: universal`, `platform: ios`, `size: 1024x1024`; the project selects `AppIcon`. This matches Xcode's supported single-size asset-catalog workflow; separate size entries or Icon Composer are not required for this asset. [Apple: configuring an app icon](https://developer.apple.com/documentation/xcode/configuring-your-app-icon/).
- **Live Activity tint:** no need to restore `black.opacity(0.35)` solely because the system composites the Lock Screen. Amber contrast is approximately 11.94:1 against Island black and 11.06:1 against the specified ink. No amber Island region fails contrast; expanded regions introduce no amber text, and the hearts remain `.red`. Actual wallpaper/Always-On rendering is unverified by the supplied captures. [Apple: Live Activity color guidance](https://developer.apple.com/design/human-interface-guidelines/live-activities?changes=l_8).
- **Grep:** `rg -n 'No .*yet|ContentUnavailableView' WorkoutTracker --glob '*.swift'` finds no `ContentUnavailableView`. Apart from the leftovers in finding 2 and a non-UI comment, the remaining matches already feed `EmptyState` (History, chart, Gym detail and the two changed machine sheets).

Standards: clear. Spec/correctness: 5 findings, highest P2. Not clear yet.
