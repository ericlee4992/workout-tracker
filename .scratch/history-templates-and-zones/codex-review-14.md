# Tickets 13 / 14 — T6 review

Reviewed `main..HEAD` on `history-14-zone-times`, `fcef729` → `c18538f` (`06c0274` plus `c18538f`). Source/diff review and visual inspection of the supplied captures; no source edits, `xcodebuild`, or `simctl`. Full UI-suite results remain pending as stated in the review request.

**A — Ticket 13, round 2: clear.** Both previous P3s are addressed. The design record describes the inherited composition and the added confirmation row; the menu, alert, and confirmation captures show the same state at default and AccessibilityL, with the new copy whole. `HistoryTemplateUITests.testTheFinishSheetStillSavesATemplate` now directly checks the prefilled name, Save, confirmation, and removal of the button; its capture shows the saved result. No remaining finding on A.

**B — Ticket 14, round 1: P3 — Assert the visibility condition after the scrolling loop.** `WorkoutTrackerUITests/RedesignScreenshotUITests.swift:340` checks only `lastRow.exists`, although the preceding loop is trying to establish `lastRow.isHittable`. If all four attempts end with Zone 3 still behind the tab bar or outside the viewport, the test can pass and attach an incomplete capture. Assert the final visibility condition; checking the row's full frame above the tab bar would enforce the ticket's “card whole” requirement more accurately. The supplied `14-05-detail-heart-rate-zones-axl.png` is complete, so this is a future regression-gate weakness, not an observed layout defect.

Otherwise B is clear against REVIEW items 1, 3, 6, 9, 11, and 12: the graph and zone summary are two peer structured cards in the List, with no nesting or per-zone cards; the new card introduces no competing command, and its colors identify zones while labels/durations retain neutral readable text. Both sizes show every zone row and duration whole. `ZoneTimeCard` preserves the finish sheet's strings, filtering, bar widths, spacing, colors, and row formatting; the only accessibility change combines each row. History reads persisted `zoneSeconds`, without recalculating historical zones from live settings, and suppresses the card when no zone has time.

The fixture uses the production zone classifier with a fixed 185 maximum, assigning 15 seconds per positive mean and none to zero buckets. Independently recomputing its deterministic generator gives `[0, 1950, 930, 645, 0, 0]`: 32:30 / 15:30 / 10:45, totaling 58:45 plus the 75-second gap. Production `WorkoutVitalsMath` attributes raw inter-sample durations and `WorkoutSummaryBuilder.capture` persists those results; the fixture is a reasonable synthetic display approximation, not a test of that production fold. Its inline claim at `HeartRateHistoryFixture.swift:109` that the series reaches the high 170s/all five zones is stale; the ticket and captures correctly describe three zones.
