clear — On boundary `2525711..94425ae`, Standards and Spec are both clear. The milestone-3 JSON example now states schema v6 and enumerates the v5/v6 additions (`.scratch/milestone-3-export/spec.md:132`), and the ticket's test record now says the typed name has its own CSV column while explicitly striking the obsolete “CSV prefers it” wording (`.scratch/milestone-9-history-and-summary/issues/02-workout-name.md:51`). A repository-wide search found no remaining `schemaVersion.*[45]`, `35 columns`, `34 columns`, or `prefers` match that incorrectly describes the current export contract: the remaining matches are historical review/ticket records, intentional v5 compatibility tests, field-introduction annotations such as D44's schema-v4 note, accurate append-only comments, or unrelated uses of “prefers.” The fix is documentation-only, introduces no new defect or smell, and `git diff --check 2525711..94425ae` passes. Standards — 0 findings; Spec — 0 findings.
