Clear. At `cb3773f..b16a233`, the provenance fixture now derives `normalizedKg` from its exact `198.4 lb` entered value through `WeightMath.normalizedKg(value:unit:)`, matching the app’s D25 normalization path and restoring a consistent atomic weight tuple; the `100 kg` set remains the heaviest while the `198.4 lb × 10` set still wins estimated 1RM, so the test continues to exercise the intended mixed-unit provenance behavior. `git diff --check` passes, and the focused `ProgressSeriesTests` run passed all 12 tests. Standards — 0 findings (clear); Spec — 0 findings (clear).
