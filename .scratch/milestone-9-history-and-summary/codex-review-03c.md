Clear — Standards: `Day.emphasis` now distinguishes all six reachable flag combinations, including `.markedFuture`; the focused test covers a real future mark and every reachable tuple. `CellStyle` is the single exhaustive presentation mapping, and marked-future is dimmed while remaining checked, tappable, and explicitly labeled. The deleted-pick test now inserts and saves the fixture in an in-memory SwiftData context, deletes it, proves `isDeleted`, and proves dismissal drops it. Spec: both round-2 findings are fully closed, with no fix-introduced defect, scope creep, new smell, or uncalled declaration found. `git diff --check 18d707b...2b5d611` passed and all 12 `WorkoutCalendarTests` passed. Standards — 0 findings; Spec — 0 findings.
